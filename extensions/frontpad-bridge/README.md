# LabelPrint FrontPad Bridge (Chrome / Edge)

**Версия: 1.4.4** (`manifest.json`)

Расширение добавляет кнопку **LP** в шапку FrontPad (слева от **ФР**),
перехватывает сохранение заказа (`order.php`) и отправляет состав на локальный webhook
**LabelPrint Pro**. Опционально включает **тёмную тему** FrontPad.

- Иконка LP: зелёная `#6AC579` при связи с LabelPrint, красная `#E74C3C` без связи.
- Клик по LP открывает окно подключения (IP/Port, автоподключение, тест webhook).
- Popup расширения — подсказка нажать кнопку LP на странице FrontPad.

На GitHub Release отдельный архив: `frontpad-bridge-1.4.4.zip`  
(в установщике/portable приложения то же расширение лежит в `extensions/frontpad-bridge/`).

## Установка / обновление

1. Запустите LabelPrint Pro. В **Настройки → Общие** укажите webhook, например `http://127.0.0.1:8765/`, сохраните, **перезапустите** приложение (слушатель стартует при запуске).
2. Chrome: `chrome://extensions` / Edge: `edge://extensions` → режим разработчика → **Загрузить распакованное** (или «Обновить» после правок).
3. Папка:
   - из репозитория: `extensions/frontpad-bridge`
   - из установленного приложения: `<install>\extensions\frontpad-bridge`
4. Откройте FrontPad **заново** (закрыть вкладку и открыть снова).
5. Справа вверху нажмите **LP** → **Подключить** (иконка должна стать зелёной).
6. Сохраните заказ в FrontPad.

Краткая памятка также в `INSTALL.txt` рядом с манифестом.

### Если «отправлено: 0»

| Статус в popup | Значение |
|----------------|----------|
| Нет «Хук активен» | Расширение не на этой вкладке / не тот домен / не обновлена вкладка |
| «Вижу order.php» но не «Отправлен» | Смотрите красную ошибку / debug-строку |
| «Пропуск: No positions» | Request body пустой или другой формат — пришлите debug |
| «Ошибка отправки» | LabelPrint не слушает webhook (не запущен / другой URL / URL не сохранён) |
| «Тест webhook OK», заказ нет | Хук не ловит сохранение — Console FrontPad: есть ли `[LabelPrint Bridge] hook active` |

## Как работает

1. Content script внедряет хук в страницу (XHR + `fetch`), heartbeat сообщает LabelPrint о живом Bridge.
2. При успешном ответе `order.php` (`result: success`) собираются:
   - **Request** → `positions` (состав)
   - **Response** → `order_id`, `order_n`, дата/время
3. Формируется JSON inbox LabelPrint и делается `POST` на webhook.

## Настройки

| Параметр | По умолчанию |
|----------|----------------|
| Webhook URL | `http://127.0.0.1:8765/` |
| Мост включён | да |
| Тёмная тема FrontPad | нет (галочка в popup / options) |

### Тёмная тема

- Включается галочкой в popup или на странице настроек расширения.
- Тёплая угольная палитра (не «негатив» / не синий chrome).
- Перекрашиваются светлые панели; цветные статусы кухни сохраняются в приглушённом виде.
- После включения/обновления расширения перезагрузите вкладку FrontPad (`Ctrl+Shift+R`).

Дубли одного `order_id` игнорируются 5 минут.

## Ограничения

- Домен: `*.frontpad.ru` (кастомный хост — добавьте в `manifest.json` → `content_scripts` / `host_permissions`).
- Срабатывает при **сохранении** заказа через `order.php`, не при простом просмотре списка.
- `positions.name` иногда приходит строкой `"A,B"` или sparse-join `,,,Блюдо,,,,Добавка с, запятыми.` — парсер сохраняет пустые слоты и склеивает «лишние» запятые в последнюю ячейку; пустые «Позиция N» больше не печатаются.
- Добавки (модификаторы) с `positions.parent` (индекс родителя, в т.ч. `"0"`) склеиваются в **одну позицию**: `name` = блюдо, `addons` / `comment` = список добавок (для шаблона «Кухня чек»).
- `positions.mod=1` означает «у товара разрешены модификаторы», а не «строка — добавка».
- `sku` = внутренний `productID` FrontPad; сопоставление с каталогом — по имени или после настройки артикулов.
- Неофициальный канал; обновление UI/API FrontPad может сломать перехват.

## Отладка

1. Popup расширения: текст ошибки / «Отправлен заказ №…».
2. `F12` на странице FrontPad → Console: нет ли блокировок CSP (редко).
3. Service worker на `chrome://extensions` → «Просмотр service worker» → логи.
4. Папка inbox: `%LocalAppData%\LabelPrintPro\orders-inbox\` — должен появиться `webhook_*.json`.
5. На главной LabelPrint — индикатор **Bridge** в блоке «Статус системы».
