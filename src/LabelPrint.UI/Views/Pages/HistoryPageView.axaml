<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:LabelPrint.UI.ViewModels"
             xmlns:dto="using:LabelPrint.Application.DTOs"
             xmlns:ui="using:LabelPrint.UI"
             x:Class="LabelPrint.UI.Views.Pages.HistoryPageView"
             x:DataType="vm:HistoryViewModel">
  <DockPanel Margin="4">
    <StackPanel DockPanel.Dock="Top" Spacing="14" Margin="0,0,0,12">
      <TextBlock Text="{Binding Title}" Classes="page-title" />
      <TextBlock Text="{Binding StatusMessage}" Classes="page-subtitle" TextWrapping="Wrap" />
      <Border Classes="app-card" Padding="12">
        <StackPanel Orientation="Horizontal" Spacing="8">
          <Button Command="{Binding LoadCommand}">
            <StackPanel Orientation="Horizontal" Spacing="8">
              <Path Classes="app-icon-sm" Data="{x:Static ui:AppIcons.Refresh}" />
              <TextBlock Text="Обновить" VerticalAlignment="Center" />
            </StackPanel>
          </Button>
          <Button Content="Ещё" Command="{Binding LoadMoreCommand}" IsEnabled="{Binding CanLoadMore}" />
          <Button Classes="accent" Command="{Binding ReprintSelectedCommand}">
            <TextBlock Text="Перепечатать"  />
          </Button>
        </StackPanel>
      </Border>
    </StackPanel>

    <Border Classes="app-panel">
      <DataGrid Classes="app-grid"
                ItemsSource="{Binding Entries}"
                SelectedItem="{Binding Selected}"
                AutoGenerateColumns="False"
                IsReadOnly="True"
                CanUserResizeColumns="True"
                GridLinesVisibility="Horizontal"
                BorderThickness="0"
                Background="Transparent"
                RowBackground="Transparent"
                ScrollViewer.HorizontalScrollBarVisibility="Auto">
        <DataGrid.Columns>
          <DataGridTextColumn Header="Дата" Binding="{Binding PrintedAt}" Width="Auto" MinWidth="100" />
          <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*" MinWidth="120" />
          <DataGridTextColumn Header="Принтер" Binding="{Binding PrinterName}" Width="Auto" MinWidth="72" />
          <DataGridTextColumn Header="Товар" Binding="{Binding ProductName}" Width="*" MinWidth="80" />
          <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="Auto" MinWidth="64" />
          <DataGridTextColumn Header="Коп." Binding="{Binding Copies}" Width="Auto" MinWidth="40" />
          <DataGridTextColumn Header="Ошибка" Binding="{Binding FailureReason}" Width="2*" MinWidth="80" />
        </DataGrid.Columns>
      </DataGrid>
    </Border>
  </DockPanel>
</UserControl>
